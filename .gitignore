node_modules/
.next/
dist/
out/
.env
.env.local
.env.*.local
*.log
.DS_Store

# face-api models (โหลดผ่านสคริปต์ — อย่าใส่ใน repo)
models/

# secrets
secrets/
*.pem
service-account*.json
# Google service account JSON ที่ Cloud Console ตั้งชื่อด้วย project prefix
facefindpic-*.json
*-credentials.json
# Setup wizard config (มี admin password + session secret)
data/config.json

# database
*.sqlite
*.sqlite-journal
data/

# build
*.tsbuildinfo
